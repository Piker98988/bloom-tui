[project]
name = "colourup"
description = "Utility to make your terminal apps stand out."
version = "0.1.3"
dependencies = [
    "colorama>=0.4.6",
]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
